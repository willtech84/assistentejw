import 'package:flutter/material.dart';

class DesignacoesScreen extends StatelessWidget {

  const DesignacoesScreen({super.key});

  @override
  Widget build(BuildContext context) {

    return Scaffold(
      appBar: AppBar(
        title: const Text("Designações"),
      ),
      body: const Center(
        child: Text("Em desenvolvimento"),
      ),
    );
  }
}
